// Canonical Family Circle logo artwork from the supplied source asset.
// Cache-busted so the deployed app does not keep an older broken image.
export const FAMILY_CIRCLE_LOGO = `${import.meta.env.BASE_URL}logo.webp?v=3`
